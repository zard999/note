// 1.代替reverse
// function Reverse(arr = []) {
//   // (a,b)：S逗号运算符，最后返回b
//   return arr.reduceRight((p, c) => (p.push(c), p), []);
// }

// console.log(Reverse([1, 2, 3, 4]));

// 代替map和filter

// 2.代替map和filter

